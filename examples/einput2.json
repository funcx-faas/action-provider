{
    "required": [
        "funcx_tasks",
        "task_count"
    ],
    "properties": {
        "funcx_tasks": {
            "description": "List of tasks to invoke",
            "type": "string"
        },
        "task_count": {
            "description": "Count of number of tasks",
            "type": "integer"
        }
    }
}